name: wire-up-pi-backend-engine-selection-from-config
description: Wire up config.backend selection in EforgeEngine.create() so that
  setting backend:pi instantiates PiBackend instead of ClaudeSDKBackend
created: 2026-03-29
compiled: 2026-03-29
mode: wiring-with-test
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-engine-wiring
    name: Wire up Pi backend selection from config in EforgeEngine.create()
    depends_on: []
    branch: wire-up-pi-backend-engine-selection-from-config/engine-wiring
    build:
      - implement
      - test-cycle
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: errand
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
