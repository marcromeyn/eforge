name: fix-crash-when-orchestration-yaml-is-missing-in-plannerstage
description: Fix crash when orchestration.yaml is missing in plannerStage by
  moving injectPipelineIntoOrchestrationYaml inside the existing try/catch
created: 2026-04-03
compiled: 2026-04-03
mode: errand
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-fix-missing-orchestration-yaml
    name: Fix crash when orchestration.yaml is missing in plannerStage
    depends_on: []
    branch: fix-crash-when-orchestration-yaml-is-missing-in-plannerstage/fix-missing-orchestration-yaml
    build:
      - implement
      - test-cycle
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
pipeline:
  scope: errand
  compile:
    - prd-passthrough
  defaultBuild:
    - implement
    - test-write
  defaultReview:
    strategy: auto
    perspectives:
      - correctness
    maxRounds: 1
    evaluatorStrictness: lenient
  rationale: This is a trivial bug fix - moving one function call inside an
    existing try/catch block in a single file, plus adding a test.
    prd-passthrough is sufficient since the PRD already specifies the exact code
    change. test-write is included because the PRD explicitly requires a new
    test case. No review cycle needed for such a minimal, well-defined change.
