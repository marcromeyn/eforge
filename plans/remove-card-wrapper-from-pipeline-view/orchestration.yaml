name: remove-card-wrapper-from-pipeline-view
description: Remove card wrapper styling from pipeline view so content aligns
  flush with surrounding elements
created: 2026-03-25
compiled: 2026-03-25
mode: errand
base_branch: main
validate:
  - pnpm type-check
  - pnpm build
plans:
  - id: plan-01-strip-card-styling
    name: Strip Card Styling from Pipeline View
    depends_on: []
    branch: remove-card-wrapper-from-pipeline-view/strip-card-styling
    build:
      - implement
    review:
      strategy: single
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: lenient
profile:
  extends: errand
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
