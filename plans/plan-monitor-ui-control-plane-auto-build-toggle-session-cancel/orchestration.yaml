name: plan-monitor-ui-control-plane-auto-build-toggle-session-cancel
description: Add auto-build toggle and session cancel controls to the monitor
  UI, wiring existing daemon API endpoints into the React frontend
created: 2026-03-23
compiled: 2026-03-23
mode: errand
base_branch: main
validate:
  - pnpm type-check
  - pnpm build
plans:
  - id: plan-01-control-plane-ui
    name: Monitor UI Control Plane — Auto-build Toggle + Session Cancel
    depends_on: []
    branch: plan-monitor-ui-control-plane-auto-build-toggle-session-cancel/control-plane-ui
    build:
      - implement
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: excursion
  description: "Multi-file feature work or refactors. Use when changes touch many
    files but are tightly coupled - e.g., type signature changes that cascade to
    consumers, interface refactors, rename-and-update-all-callers. The key test:
    can every piece pass type-check independently? If not, use excursion."
  compile:
    - planner
    - plan-review-cycle
