name: plan-npm-package-distribution
description: Add library entry point, type declarations, and npm distribution
  config so eforge works as both a CLI and an importable library
created: 2026-03-20
compiled: 2026-03-20
mode: errand
base_branch: main
validate:
  - pnpm build
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-npm-distribution
    name: npm Package Distribution
    depends_on: []
    branch: plan-npm-package-distribution/npm-distribution
    build:
      - implement
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: errand
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
