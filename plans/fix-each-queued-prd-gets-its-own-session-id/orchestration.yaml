name: fix-each-queued-prd-gets-its-own-session-id
description: Move session lifecycle into engine runQueue() so each queued PRD
  gets its own session ID in the monitor
created: 2026-03-18
compiled: 2026-03-18
mode: errand
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-per-prd-session
    name: Per-PRD Session Lifecycle in Queue Mode
    depends_on: []
    branch: fix-each-queued-prd-gets-its-own-session-id/per-prd-session
profile:
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
  build:
    - - implement
      - doc-update
    - review
    - review-fix
    - evaluate
  agents: {}
  review:
    strategy: auto
    perspectives:
      - code
    maxRounds: 1
    evaluatorStrictness: standard
